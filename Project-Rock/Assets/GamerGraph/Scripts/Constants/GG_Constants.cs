using UnityEngine;

public class GG_Constants : MonoBehaviour
{
    public enum ProcessRequestOptions
    {
        Accepted,
        Rejected
    };

    public enum FriendRequestStatus
    {
        Pending
    };
}
